/* You don't need to touch this, like ever */
export const STORE_NAME = "player_data";

/* Increment this when breaking changed to player data is made */
export const STORE_VERSION = 1;

export type PlayerData = {
    /* Fill this in */
};

export const DEFAULT_DATA = {
    /* Fill this in */
} as PlayerData;
